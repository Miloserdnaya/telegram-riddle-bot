# 🚂 Деплой бота на Railway.app

## Шаг 1: Загрузка на GitHub

### 1.1. Инициализация Git репозитория (уже сделано)

```bash
cd /Users/afinamiloserdnaya/ripple
git init
```

### 1.2. Добавление файлов

```bash
# Добавить все файлы
git add .

# Проверить, что добавлено
git status
```

### 1.3. Первый коммит

```bash
git commit -m "Initial commit: Telegram riddle bot"
```

### 1.4. Создание репозитория на GitHub

1. Откройте https://github.com/new
2. Название репозитория: `telegram-riddle-bot` (или любое другое)
3. Выберите **Public** или **Private**
4. **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)
5. Нажмите **Create repository**

### 1.5. Подключение и загрузка

GitHub покажет команды. Выполните:

```bash
# Добавить удаленный репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/telegram-riddle-bot.git

# Переименовать ветку в main (если нужно)
git branch -M main

# Загрузить код
git push -u origin main
```

## Шаг 2: Деплой на Railway

### 2.1. Регистрация на Railway

1. Откройте https://railway.app
2. Нажмите **Login** и войдите через GitHub
3. Подтвердите доступ к вашему GitHub аккаунту

### 2.2. Создание проекта

1. Нажмите **New Project**
2. Выберите **Deploy from GitHub repo**
3. Выберите ваш репозиторий `telegram-riddle-bot`
4. Railway автоматически определит Python проект

### 2.3. Настройка переменных окружения

1. В проекте Railway откройте вкладку **Variables**
2. Добавьте переменную:
   - **Key**: `BOT_TOKEN`
   - **Value**: `8552794244:AAEEMnwCkRmNWV8hC_wyU26B-0OUheHFZjc`

3. (Опционально) Если используете Google Sheets:
   - **Key**: `GOOGLE_SHEET_ID`
   - **Value**: ваш ID таблицы
   - **Key**: `GOOGLE_CREDENTIALS_FILE`
   - **Value**: `credentials.json`

### 2.4. Настройка деплоя

Railway автоматически:
- ✅ Определит Python проект
- ✅ Установит зависимости из `requirements.txt`
- ✅ Запустит `bot.py`

Если нужно настроить вручную:

1. Откройте **Settings** → **Deploy**
2. **Start Command**: оставьте пустым (Railway сам определит)
3. Или укажите: `python3 bot.py`

### 2.5. Загрузка credentials.json (если используется Google Sheets)

1. В проекте Railway откройте **Variables**
2. Нажмите **New Variable**
3. **Key**: `GOOGLE_CREDENTIALS` (или другое имя)
4. **Value**: скопируйте **весь** JSON из вашего `credentials.json`
5. В коде можно добавить логику для создания файла из переменной окружения

Или используйте **Railway Volume**:
1. Создайте Volume в проекте
2. Загрузите `credentials.json` в Volume
3. Укажите путь в переменной `GOOGLE_CREDENTIALS_FILE`

### 2.6. Деплой

1. Railway автоматически начнет деплой после подключения репозитория
2. Следите за логами в разделе **Deployments**
3. После успешного деплоя бот будет работать 24/7!

## Шаг 3: Проверка работы

1. Откройте вкладку **Deployments**
2. Нажмите на последний деплой
3. Откройте **Logs** - там будут логи бота
4. Проверьте, что бот отвечает в Telegram

## 🔧 Дополнительные настройки

### Обновление кода

После каждого `git push` Railway автоматически перезапустит бот с новым кодом.

### Просмотр логов

В Railway → **Deployments** → выберите деплой → **Logs**

### Переменные окружения

Все секретные данные (токены, ключи) храните в **Variables**, а не в коде!

## ⚠️ Важно

- **Не коммитьте** `.env` файл (он в `.gitignore`)
- **Не коммитьте** `credentials.json` (он в `.gitignore`)
- **Не коммитьте** `*.db` файлы (они в `.gitignore`)
- Используйте **Variables** в Railway для секретов

## 📝 Файлы для Railway

Railway автоматически использует:
- `requirements.txt` - для установки зависимостей
- `bot.py` - как точку входа (можно указать в настройках)

## 🎉 Готово!

После деплоя бот будет работать 24/7 на серверах Railway!

