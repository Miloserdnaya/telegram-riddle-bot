# 🚀 Бот запущен и работает!

## ✅ Текущий статус

Бот настроен для автозапуска через **launchd** и работает в фоновом режиме.

## 📋 Управление ботом

### Проверить, что бот работает:
```bash
ps aux | grep "Python.*bot.py" | grep -v grep
```

### Остановить бота:
```bash
launchctl unload ~/Library/LaunchAgents/com.riddle.bot.plist
```

### Запустить бота:
```bash
launchctl load ~/Library/LaunchAgents/com.riddle.bot.plist
```

### Перезапустить бота:
```bash
launchctl unload ~/Library/LaunchAgents/com.riddle.bot.plist
sleep 2
launchctl load ~/Library/LaunchAgents/com.riddle.bot.plist
```

### Просмотр логов:
```bash
# Основной лог
tail -f ~/ripple/bot.log

# Лог ошибок
tail -f ~/ripple/bot.error.log
```

## ⚠️ Важно

**Бот работает только когда ваш Mac включен.** 

Для работы 24/7 даже при выключенном ноутбуке нужен облачный сервер (VPS). См. `SETUP_24_7.md` для инструкций по деплою на облачный сервер.

## 🔄 Автозапуск

Бот автоматически запускается:
- ✅ При включении компьютера
- ✅ При перезагрузке системы
- ✅ При сбое (автоматический перезапуск)

## 📁 Файлы

- `com.riddle.bot.plist` - конфигурация launchd
- `start_bot.sh` - скрипт запуска
- `bot.log` - основной лог
- `bot.error.log` - лог ошибок

